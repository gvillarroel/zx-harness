- URL: https://github.com/cli/cli/issues/3239
  Summary: Issue #3239 reports a Windows-specific output-mode discrepancy in `gh`: on Windows 10 with `gh` 1.7.0, `gh issue view -c` shows the normal rich issue view in the terminal but, when redirected to a file, emits only comment data in a different plain format or nothing if there are no comments. This is useful as prior evidence that `gh` can switch formatting/content based on whether stdout is treated as a terminal, which is a plausible clue for terminal-detection or output-rendering bugs behind the unusual `gh repo create` display in Alacritty and Windows Terminal.
- URL: https://github.com/cli/cli/issues/3526
  Summary: Issue #3526 is a Windows support tracking issue that gives relevant context for #13054: it explicitly calls out the large matrix of Windows shells and terminal emulators, including Windows Terminal and Alacritty, and notes that different combinations can cause unicode rendering, color palette, and output-display problems. It also identifies a likely gap in `gh`: poor detection of the active Windows terminal environment, and proposes improving environment detection plus documenting blessed terminal/shell combinations, which is a concrete root-cause clue and implementation direction for unusual `gh repo create` output on Windows terminals.
- URL: https://github.com/alacritty/alacritty/issues/2592
  Summary: This page is useful because it reports a Windows multiline rendering bug in Alacritty and notes the same behavior in Windows Terminal, which suggests the problem may be terminal/TERM handling rather than `gh` itself. The issue text specifically says ncurses-based programs behave incorrectly unless `TERM=cygwin`, while ConEmu does not show the problem, providing a concrete workaround clue and a root-cause direction around Windows terminal compatibility and terminal capability detection.
