interface:
  display_name: "zx Workflow Author"
  short_description: "Generate gated code-assistant zx workflows"
  default_prompt: "Use $zx-workflow-author to generate a standalone zx script for this recurring repository problem type, accepting each issue, review, or triage target at runtime and solving it through isolated code-assistant contexts, selected skills, and executable gates."
