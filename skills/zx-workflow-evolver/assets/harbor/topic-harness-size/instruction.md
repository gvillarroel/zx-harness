# Compact topic harnesses: __TASK_ID__

Use the installed `zx-workflow-author` skill to scaffold the topic workflow.

The result must accept the single topic `__TOPIC__`, provide distinct Codex, Copilot, pi, and
OpenCode entrypoints, process only new source versions, and validate an Open Knowledge Format
library. Keep each executable below `__MAX_SCRIPT_BYTES__` bytes and delegate deterministic work
to `jq`, `rg`, `fd`, `git`, and `know`.
