# zx-example-author compare rerun

Date: 2026-03-28

Run:
- Eval ID: `eval-Fag-2026-03-28T18:49:18`
- Compare run: `C:\Users\villa\dev\skill-arena\results\zx-example-author-compare\2026-03-28T18-49-10-209Z-compare`
- Score: `6/12`
- Passed: `6`
- Failed: `6`
- Errors: `0`
- Duration: `506727 ms`

## What changed before rerun

- Compare assertions now resolve files from the active provider workspace instead of the first cell workspace.
- Compare provider metadata now exposes the active workspace path.
- Compare runs now always clean temporary execution roots and clear the git source cache at the end.
- Matrix summaries no longer count assertion failures as execution errors.

## Outcome

- `skill` profile passed `6/6`.
- `no-skill` profile failed `6/6`.
- The result is now meaningful: passes and fails are tied to each cell's own workspace, with no cross-cell contamination.

## Failure reasons

### `codex-mini` + `no-skill`

- `hello-name`: file missed the required `Error` handling string from the recipe.
- `hello-cop`: file missed the exact `copilot -p 'ping' --model gpt-5-mini` command.
- `gh-involved-repos`: `repo.ts` missed the required `console.log(\`name: \${name}\`);` formatter.

### `gpt-5.4` + `no-skill`

- `hello-name`: file missed the required `Error` handling string from the recipe.
- `hello-cop`: file missed the exact `copilot -p 'ping' --model gpt-5-mini` command.
- `gh-involved-repos`: `repo.ts` missed the required `console.log(\`name: \${name}\`);` formatter.

## Notes

- The benchmark now distinguishes recipe-following from plausible-but-different implementations.
- The compare summary previously showed false `errors`; after the matrix fix the final run reports `Errors: 0`.
