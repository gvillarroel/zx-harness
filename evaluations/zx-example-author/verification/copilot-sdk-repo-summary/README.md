# Copilot SDK Repo Summary

Small zx wrapper plus one TypeScript summarizer.

## Run

Install local dependencies first.

```bash
npm install
zx index.mjs /path/to/repo
```

The summarizer writes markdown and JSON artifacts under `run/`.
